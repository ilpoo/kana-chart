import React, { memo } from "react";

export default memo(function Header () {
  return (
    <header>
      <div className="menuTitle">
        <h1>Kana Chart</h1>
      </div>
    </header>
  );
});
